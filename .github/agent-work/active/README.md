# Active Work

Features and tasks currently in progress. Agents should check this directory at the start of every session.

## Rules
- Only ONE feature should be actively worked at a time per agent session
- Update task checkboxes as steps complete
- Record decisions and blockers in real-time
- Move to `completed/` when all acceptance criteria pass
